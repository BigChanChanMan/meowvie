# TODO

## 焦点管理优化（触发后优先做）

**现状**：全部键盘输入由 `src/App.tsx` 顶层的 `useInput` 用 `view` / `selected` / `settingsIndex` 状态机手写分发。同一屏只有一个活跃区域；首页排行榜入口目前靠「空搜索框 + 数字键 1-4」绕过焦点冲突。

**触发条件**：出现以下任一需求时，优先做本优化：
- 同一屏多个交互区需要并排抢占键盘（如搜索框与榜单菜单 Tab 循环切换焦点）
- 详情页要切分「简介 / 演职员表 / 剧照」等多个子面板
- 模态框、多栏布局、多个可聚焦的按钮 / 列表

**优化方向**：引入焦点管理 —— 维护「当前焦点在哪个元素」的状态，用 Tab / ↑↓ 移动焦点，按键按焦点路由。ink 提供 `useFocus` / `useFocusManager` 可配合实现。