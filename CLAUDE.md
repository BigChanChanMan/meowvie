## Agent skills

### Issue tracker

本仓库的 issue 托管在 GitHub（`BigChanChanMan/meowvie`），各技能统一使用 `gh` CLI。参见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的规范标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。参见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：仓库根目录的 `CONTEXT.md` + `docs/adr/`。参见 `docs/agents/domain.md`。